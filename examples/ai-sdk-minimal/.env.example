# A GitHub token with the `repo`/`public_repo` scope. nominee resolves this
# FRESH at each tool call via the function strategy in agent.ts.
GITHUB_TOKEN=ghp_xxx

# Any OpenAI-compatible gateway. This example uses OpenRouter.
OPENROUTER_API_KEY=sk-or-xxx
